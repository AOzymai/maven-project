pipeline {
  agent any
  stages {
    stage ('Initialize') {
      steps {
       s ```
         echo "PATH = ${PATH}"
         echo "JAVA_HOME = ${JAVA_HOME}"
       ```
      }
    }
    stage ('Build') {
      steps {
        echo "Hello, Jenkins!"
      }
    }
  }
} 
  
